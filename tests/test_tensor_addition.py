import sys
import os

# Add the src directory to the Python path
sys.path.append(os.path.join(os.path.dirname(__file__), 'src'))

from integrated_framework import టెన్సర్, టెన్సర్_సృష్టించు, టెన్సర్_జోడించు

def పరీక్ష_టెన్సర్_జోడించు():
    print("టెన్సర్ జోడింపు పరీక్ష ప్రారంభం...")

    # సాధారణ కేసు పరీక్ష
    try:
        టెన్సర్1 = టెన్సర్_సృష్టించు([1, 2, 3, 4], (2, 2))
        టెన్సర్2 = టెన్సర్_సృష్టించు([5, 6, 7, 8], (2, 2))
        ఫలితం = టెన్సర్_జోడించు(టెన్సర్1, టెన్సర్2)
        ఆశించిన_ఫలితం = [6, 8, 10, 12]

        if ఫలితం.డేటా == ఆశించిన_ఫలితం:
            print("సాధారణ కేసు పరీక్ష విజయవంతం")
        else:
            print(f"సాధారణ కేసు పరీక్ష విఫలమైంది. ఆశించింది: {ఆశించిన_ఫలితం}, వచ్చింది: {ఫలితం.డేటా}")
    except Exception as e:
        print(f"సాధారణ కేసు పరీక్ష విఫలమైంది: {e}")

    # భిన్న ఆకారాల పరీక్ష
    try:
        టెన్సర్1 = టెన్సర్_సృష్టించు([1, 2, 3], (3,))
        టెన్సర్2 = టెన్సర్_సృష్టించు([4, 5, 6, 7], (4,))
        ఫలితం = టెన్సర్_జోడించు(టెన్సర్1, టెన్సర్2)
        print("భిన్న ఆకారాల పరీక్ష విఫలమైంది: దోషం ఎత్తబడలేదు")
    except ValueError as e:
        print(f"భిన్న ఆకారాల పరీక్ష విజయవంతం: {e}")
    except Exception as e:
        print(f"భిన్న ఆకారాల పరీక్ష విఫలమైంది: అనుకోని దోషం - {e}")

    print("టెన్సర్ జోడింపు పరీక్ష ముగిసింది.")

if __name__ == "__main__":
    పరీక్ష_టెన్సర్_జోడించు()
